services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
      args:
        - NEXT_PUBLIC_CONVEX_URL=https://hidden-aardvark-287.convex.cloud
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
